<!DOCTYPE html>
<html>
  <head>
    <title>PianoApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <div class="header-logo">
        <% if @current_user %>
      <%= link_to("PianoApp", "/posts/index") %>
    <% else %>
      <%= link_to("PianoApp", "/") %>
    <% end %>
      </div>
      <ul class="header-menus">


        <% if @current_user %>
        <li>

        <!-- コードを追加し、中のユーザーのidを表示してください -->
        <%= link_to(@current_user.name, "/users/#{@current_user.id}") %>
      </li>

        <li>
          <%= link_to("投稿一覧", "/posts/index") %>
        </li>
        <li>
          <%= link_to("新規投稿", "/posts/new") %>
        </li>
        <li>
          <%= link_to("ユーザー一覧", "/users/index") %>
        </li>
        <li>
        <!-- ログアウト用のリンクを追加してください -->
        <%= link_to("ログアウト", "/logout", {method: :post}) %>
      </li>
      <% else %>
      <li>
        <%= link_to("PianoAppとは", "/about") %>
      </li>
        <li>
          <%= link_to("新規登録", "/signup") %>
        </li>
        <li>
          <!-- ログインページへのリンクを作成してください -->
          <%= link_to("ログイン", "/login") %>
        </li>
        <% end %>
      </ul>
      <span class="fa fa-bars" id="header-icon"></span>
    </header>
    <!-- header-backgroundをheaderの子要素にすると、
        z-indexがローカルになるためtopページで支障が出る -->
    <div class="header-background"></div>
    <% if flash[:notice]%>
      <div class="flash">
        <%= flash[:notice]%>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
